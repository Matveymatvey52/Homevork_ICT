// Группа 212, Корепин Матвей
// Домашнее задание 3 (OTUS, C++): "Угадай число" с таблицей рекордов
//
// Заголовочный файл high_scores.h.
// Здесь объявлены функции для работы с файлом таблицы рекордов:
// добавление нового результата и чтение/печать итоговой таблицы.
// Вынесены отдельно от main.cpp, чтобы логика работы с файлом
// не смешивалась с логикой самой игры.

#pragma once

#include <string>

// Имя файла, в который сохраняется таблица рекордов.
// Объявлена как constexpr, чтобы значение было единым для всей программы.
constexpr const char* kHighScoresFilename = "high_scores.txt";

// Добавляет в конец файла таблицы рекордов новую строку с именем
// игрока и числом попыток, которое ему потребовалось.
void append_high_score(const std::string& user_name, int attempts_count);

// Читает файл таблицы рекордов целиком и выводит на экран итоговую
// таблицу: для каждого уникального имени игрока — только его лучший
// (минимальный) результат. Соответствует дополнительному заданию 3.
void print_high_scores();
